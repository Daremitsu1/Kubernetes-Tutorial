# Kubernetes-Tutorial

Folder Structure:
```
myapp/
    app.py
    Dockerfile
    requirements.txt
    kubernetes/
        deployment.yaml
        service.yaml
```

You can then make the script executable with:

`chmod +x deploy.sh`

And run it with:

`./deploy.sh`
